{
  "name": "high-perf-syntax-highlighter-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": "18.20.8 || ^20.3.0 || >=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter high-perf-syntax-highlighter test && pnpm --filter high-perf-syntax-highlighter test:browser",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
